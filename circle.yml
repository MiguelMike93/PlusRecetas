  machine:
    php:
     version: 7.0.11

  dependencies:
    pre:


#     composer require "phpunit/phpunit=5.5.*"
  #   - wget https://phar.phpunit.de/phpunit-6.1.phar
   #  - php phpunit-6.1.phar --version
#    pre:
#      - dir
#      - cp composer.json /opt/circleci/php/$(phpenv global)/etc/conf.d/
#      - dir /opt/circleci/php/$(phpenv global)/etc/conf.d/
#    - composer update

  test:
    override:
    - dir -a
    - phpunit RecetasPlus/PUconsultas.php
#    - phpunit Test/PUConexion.php
#    - phpunit Test/PUDataDB.php

#     - php Test/PUConexionFree.php



  deployment:
    production: 
      branch: Development
      commands: 
      - ls -al ~/.ssh
      - cat  ~/.ssh/id_heroku.com
      - cat  ~/.ssh/id_heroku.com.pub
      #- git clone https://github.com/MiguelMike93/PlusRecetas.git
      #- cd RecetasPlus
      - git config --global user.email "miguel.beltran01@uptc.edu.co"
      - git config --global user.name "MiguelMike93"
      - git checkout master
      - git branch
      #- git reset --hard origin/master
      #- composer update
      #- git merge origin/Development -m "Integracion3 desde Circle CI"
      - git add .
      #- git add composer lock 
      #- git pull origin master
      - git merge Development -m "Merge"
      - git commit -m "Integracion desde Circle CI"
      #- git remote set-url origin https://MiguelMike93:miguel1993@github.com/MiguelMike93/PlusRecetas.git
      - git push origin master  
      #- cat  ~/.ssh/authorized_keys
      #- cat  ~/.ssh/build_key.rsa
      #- ssh-keygen -t dsa
      #- ls -al ~/.ssh
      #- cat  ~/.ssh/id_circleci_github
      #- cat  ~/.ssh/id_heroku.com
      #- cat  ~/.ssh/id_heroku.com.pub

      #- git push git@heroku.com:hoobyonthego.git $CIRCLE_SHA1:master
      #- ./merge_to_master.sh
      #- ls -al ~/.ssh
      #- ssh-keygen -t rsa -b 4096 -C "Nicolay.JG@Outlook.com" | \n | \n

      #- git remote -v
      #- git remote set-url website abc@***.com
      #- git config --global user.email "miguel.beltran01@uptc.edu.co"
      #- git config --global user.name "MiguelMike93"
      #- git checkout master
    # - git branch
      #- git reset --hard origin/master
      #- git merge ConfigCircleCI -m "Merge"
      #- git push origin master
      #hghghghghghgh